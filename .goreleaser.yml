# GoReleaser: build for linux/amd64 (pure Go; no CGO).
version: 2

project_name: purgebot

builds:
  - id: purgebot
    main: ./cmd/purgebot
    env:
      - CGO_ENABLED=0
    ldflags:
      - -X github.com/keshon/purge-bot/internal/version.Version={{.Version}}
    goos:
      - linux
    goarch:
      - amd64

archives:
  - formats: [tar.gz]

checksum:
  name_template: checksums.txt
